const errorMessages = {
  noToken: "Access denied, No token provided",
  invalidToken: "Access denied, Invalid token",
};

const errorCodes = {
  unAuthorized: 401,
  badRequest: 400,
};

module.exports = {
  errorMessages,
  errorCodes,
};
